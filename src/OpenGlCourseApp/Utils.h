#pragma once

//void for_each_loop(params, elements, element, function);

/*
template<typename T, typename H, typename F>
void for_each_loop(T _parameters, H _elements, F&& _loop_function)
{
	for (auto element : _elements)
	{
		_loop_function(element, *params);
	}
}
*/